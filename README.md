# TimeSeries
The application processes and stores solar power plant time-series data via a REST API,  supports versioning of time-series entries with a safety window mechanism,  and provides a frontend interface for uploading and visualizing the data.
